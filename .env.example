# Copie este arquivo para ".env" na raiz do projeto e preencha sua API key.

# Chaves e URLs (preencha conforme seu ambiente)

# Chave da API (usada por OpenAI e provedores compatíveis)
OPENAI_API_KEY=

# Base URL para provedores compatíveis com OpenAI (obrigatório para openaiCompatible)
# Ex.: https://api.openai.com/v1 ou a URL do seu proxy compatível
OPENAI_BASE_URL=

# Modelo padrão para os testes reais (ex.: gpt-4o-mini, deepseek-chat, etc.)
OPENAI_MODEL=gpt-4o-mini
