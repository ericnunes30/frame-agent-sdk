# Use a imagem oficial do Node.js (versão leve)
FROM node:20-alpine

# Define o diretório de trabalho
WORKDIR /app

# Instala as dependências do sistema necessárias para rodar node-gyp ou processos básicos se necessário
# (Geralmente não precisa para este pacote, mas é boa prática em alpine)
RUN apk add --no-cache python3 make g++

# O comando que será executado quando o container iniciar
# Usamos npx para garantir a versão mais recente ou instalamos globalmente
# O flag -y aceita os prompts automaticamente
ENTRYPOINT ["npx", "@upstash/context7-mcp@latest"]